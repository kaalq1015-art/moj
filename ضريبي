477013494
1096035066
1418-04-11
540450694

اللي فوق بيانات الوكيل 

والي تحت خانة ادخال الوكيل ابيها نفسها بس بدال ما أعبيها انا تكون معبيه وجاهزة مجرد ما اضغط البوكمارك يعبيها ويضغط الباقي كأني سويتها انا
javascript:(function(){    const d = document;    const oldUI = d.getElementById('agency-pro-ui');    if (oldUI) oldUI.remove();    const style = d.createElement('style');    style.innerHTML = `        #agency-pro-ui {            position: fixed; top: 40px; right: 40px; width: 360px;            background: rgba(255, 255, 255, 0.96); z-index: 2147483647;             border-radius: 20px; box-shadow: 0 20px 50px rgba(0,0,0,0.15);            font-family: 'Segoe UI', system-ui, sans-serif;            direction: rtl; text-align: right; user-select: none;            backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.5);            overflow: hidden; display: flex; flex-direction: column;            animation: slideIn 0.4s cubic-bezier(0.16, 1, 0.3, 1);        }        @keyframes slideIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }        .ui-header {            background: linear-gradient(135deg, #064e3b 0%, #059669 100%);            padding: 16px 20px; color: white; cursor: move;            display: flex; justify-content: space-between; align-items: center;        }        .ui-header h3 { margin: 0; font-size: 15px; font-weight: 700; display: flex; align-items: center; gap: 8px; }        .ui-body { padding: 22px; }        .field-group { margin-bottom: 14px; }        .field-label { display: block; font-size: 11px; font-weight: 700; color: #475569; margin-bottom: 6px; }        .ui-input {            width: 100%; padding: 10px 14px; border: 2px solid #f1f5f9; border-radius: 12px;            font-size: 14px; transition: all 0.2s; box-sizing: border-box; background: #f8fafc;        }        .ui-input:focus { outline: none; border-color: #10b981; background: #fff; box-shadow: 0 0 0 4px rgba(16, 185, 129, 0.1); }        .ui-btn-main {            width: 100%; background: #059669; color: white; border: none; padding: 14px;            border-radius: 14px; font-weight: 700; cursor: pointer; transition: 0.3s;            font-size: 15px; margin-top: 10px; box-shadow: 0 10px 15px -3px rgba(5, 150, 105, 0.3);        }        .ui-btn-main:hover { background: #047857; transform: translateY(-1px); box-shadow: 0 15px 20px -5px rgba(5, 150, 105, 0.4); }        .ui-close { background: none; border: none; color: white; cursor: pointer; font-size: 16px; opacity: 0.7; }        .ui-close:hover { opacity: 1; color: #f87171; }    %60;    d.head.appendChild(style);    const ui = d.createElement('div');    ui.id = 'agency-pro-ui';    ui.innerHTML = %60        <div class="ui-header" id="ui-drag-handle">            <h3><span>📑</span> إدخال بيانات الوكيل</h3>            <button class="ui-close" id="ui-close-btn">✕</button>        </div>        <div class="ui-body">            <div class="field-group">                <label class="field-label">رقم الوكالة</label>                <input type="text" id="poa_num" class="ui-input" placeholder="00000000">            </div>            <div class="field-group">                <label class="field-label">هوية الوكيل</label>                <input type="text" id="agent_id" class="ui-input" placeholder="10xxxxxxxx">            </div>            <div class="field-group">                <label class="field-label">تاريخ الميلاد (YYYY-MM-DD)</label>                <input type="text" id="agent_dob" class="ui-input" placeholder="1990-01-01">            </div>            <div style="display:grid; grid-template-columns: 1fr 1fr; gap:10px;">                <div class="field-group">                    <label class="field-label">الجوال</label>                    <input type="text" id="agent_mob" class="ui-input" placeholder="05xxxxxxxx">                </div>                <div class="field-group">                    <label class="field-label">البريد</label>                    <input type="email" id="agent_mail" class="ui-input" placeholder="test@mail.com">                </div>            </div>            <button id="fill_btn" class="ui-btn-main">إضافة الوكيل</button>        </div>    %60;    d.body.appendChild(ui);    let isDragging = false, offset = { x: 0, y: 0 };    const handle = d.getElementById('ui-drag-handle');    handle.onmousedown = (e) => {        isDragging = true;        offset.x = e.clientX - ui.offsetLeft;        offset.y = e.clientY - ui.offsetTop;    };    d.onmousemove = (e) => {        if (!isDragging) return;        ui.style.left = (e.clientX - offset.x) + 'px';        ui.style.top = (e.clientY - offset.y) + 'px';        ui.style.right = 'auto';    };    d.onmouseup = () => isDragging = false;    d.getElementById('ui-close-btn').onclick = () => { ui.remove(); style.remove(); };        d.getElementById('fill_btn').onclick = function() {        const poa = d.getElementById('poa_num').value.trim();        const id = d.getElementById('agent_id').value.trim();        const dob = d.getElementById('agent_dob').value.trim();        const mob = d.getElementById('agent_mob').value.trim();        const mail = d.getElementById('agent_mail').value.trim();        if (!poa || !id || !dob) return alert('الرجاء إدخال رقم الوكالة والهوية وتاريخ الميلاد');        const mobFormatted = "+966" + mob.replace(/^(0|966|\+966)/, '');        const triggerInput = (selector, value) => {            const el = d.querySelector(selector);            if (!el) return;            const setter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set;            setter.call(el, value);            el.dispatchEvent(new Event('input', { bubbles: true }));            el.dispatchEvent(new Event('change', { bubbles: true }));        };        triggerInput('input[name="poaNumber"]', poa);        triggerInput('input[name="Identity"]', id);        triggerInput('input[name="DateOfBirth"]', dob);        triggerInput('input[name="email"]', mail);        triggerInput('input[type="tel"]', mobFormatted);        setTimeout(() => {            const vBtn = Array.from(d.querySelectorAll('button')).find(b =>                b.innerText.includes('التحقق') || b.classList.contains('bg-bluerega')            );            if (vBtn) vBtn.click();        }, 300);    };})();
